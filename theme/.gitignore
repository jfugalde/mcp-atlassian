# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Dependencies (Theme Check, extensions, or local tooling)
node_modules/

# Shopify CLI (local dev — session and sync metadata; do not commit)
.shopify/

# Packaged theme drops
release
*.zip
