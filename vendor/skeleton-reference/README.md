# Skeleton reference (not deployed)

`Macross-V1.0.1/` is a **Shopify skeleton theme** snapshot kept for comparison with upstream. It is **not** part of the live storefront.

**Deployable theme:** only files directly under [`../theme/`](../theme/) (Liquid, `config/`, `sections/`, etc.). Use Shopify CLI and theme-check against that directory.
