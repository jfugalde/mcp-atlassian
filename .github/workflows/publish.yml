# Disabled: inherited from mcp-atlassian fork. This client repo must not publish to PyPI.
# Re-enable only if you intentionally own the PyPI package and configure secrets.

name: Publish MCP-Atlassian to PyPI (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: |
          echo "PyPI publish is disabled for Farmacia_Macross. Use ryu-platform if you maintain mcp-atlassian releases."
          exit 0
