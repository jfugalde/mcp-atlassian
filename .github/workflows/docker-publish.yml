# Disabled: inherited from upstream fork — avoids unintended ghcr.io image builds for this client repo.

name: Docker Publish (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: |
          echo "Docker publish disabled for Farmacia_Macross."
          exit 0
